import { toDo } from './render.js';
import { setUser, generateUser, getUser } from '../utils';

function loadUser(){

    // getUser from localStorage
    const user = getUser();
    // Update DOM with user info 
}